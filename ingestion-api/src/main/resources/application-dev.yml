# Profil de développement local.
# Activer avec : SPRING_PROFILES_ACTIVE=dev mvn spring-boot:run
# - Désactive l'auth sur /api/v1/sync/** (cf. IngestionSecurityConfig#devFilterChain)
#   pour les tests locaux : pas besoin de clé API en dev.
spring: {}
