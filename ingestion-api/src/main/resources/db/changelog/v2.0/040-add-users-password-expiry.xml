<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.27.xsd">

    <!--
      Date d'expiration du mot de passe (optionnelle). NULL = pas d'expiration.
      Si renseignée et dépassée, le login est refusé : seul un admin peut
      prolonger la date ou réinitialiser le mot de passe (le « mot de passe
      oublié » self-service ne contourne pas l'expiration).

      À NE PAS confondre avec password_expired (booléen) qui force le simple
      changement du mot de passe temporaire à la première connexion.
    -->
    <changeSet id="040-add-users-password-expires-at" author="itechciv" failOnError="true">
        <addColumn tableName="users" schemaName="auth">
            <column name="password_expires_at" type="TIMESTAMP"
                    remarks="Expiration du mot de passe (NULL = jamais). Dépassée → login refusé."/>
        </addColumn>
        <rollback>
            <dropColumn tableName="users" schemaName="auth" columnName="password_expires_at"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
