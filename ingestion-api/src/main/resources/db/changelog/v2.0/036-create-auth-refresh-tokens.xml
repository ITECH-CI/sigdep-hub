<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.27.xsd">

    <changeSet id="036-create-auth-refresh-tokens" author="itechciv" failOnError="true">
        <createTable tableName="refresh_tokens" schemaName="auth"
                     remarks="Refresh tokens (TTL 7j). L'id UUID est la valeur opaque renvoyée au client en JSON.">
            <column name="id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="user_id" type="BIGINT">
                <constraints nullable="false"
                             foreignKeyName="fk_refresh_tokens_user"
                             references="auth.users(id)" deleteCascade="true"/>
            </column>
            <column name="expires_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="revoked" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <createIndex tableName="refresh_tokens" schemaName="auth" indexName="idx_refresh_tokens_user">
            <column name="user_id"/>
        </createIndex>

        <rollback>
            <dropTable tableName="refresh_tokens" schemaName="auth"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
