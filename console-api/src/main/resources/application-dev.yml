# Profil de développement local.
# Activer avec : SPRING_PROFILES_ACTIVE=dev mvn spring-boot:run
#
# L'auth v2.0 (JWT HS256) est active comme en prod ; en dev on s'appuie sur
# le SUPER_ADMIN seedé via SIGDEP_ADMIN_EMAIL/SIGDEP_ADMIN_PASSWORD et le
# secret par défaut de app.jwt.secret. Aucune dépendance externe (plus de
# Keycloak/JWK à récupérer au boot).
spring: {}
